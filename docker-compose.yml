version: '3'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - src/:/usr/src/app/
    ports:
      - 8000:8000
    depends_on:
      - redis

  redis:
    restart: always
    image: redis:latest
    expose:
      - "6379"
